"""Shared library for the serverless ETL pipeline Lambda functions.

Packaged as a Lambda layer (see ``make package``), so every module here is
importable as ``shared.<module>`` from any function in this project.
"""
